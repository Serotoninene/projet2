<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Profile page</title>
</head>
<body>

{% for agent in agents  %}

    {% if agent.published %}

        {#Pour renvoyer à la page de l'agent, il faut utiliser son id + la fonction path de Symfony#}

        <h1><a href= "{{ path('page_profile', {'id' : agent.id}) }} " >{{agent.lastName}} {{ agent.firstName}}</a></h1>
    {% endif %}

{% endfor %}


</body>
</html>